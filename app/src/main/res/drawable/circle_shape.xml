<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/primaryColor"/> <!-- Circle color -->
    <stroke android:width="0.5dp" android:color="@android:color/black"/> <!-- Black outline -->
    <size
        android:width="24dp"
        android:height="24dp"/>
</shape>
